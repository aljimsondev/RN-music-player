import React from 'react';
import {Text, View} from 'react-native';
import {MainProps} from '../lib/Types/types';

export default function SongSearch(props: MainProps) {
  return (
    <View>
      <Text>Song Search</Text>
    </View>
  );
}
